# Completion Records

This directory contains records of completed tasks, features, and refactors.
These files are **NOT** auto-loaded by the agent to save tokens.
Load them only when you need history on a specific feature.
